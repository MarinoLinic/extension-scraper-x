{
  "name": "x-archive",
  "version": "1.0.0",
  "private": true,
  "description": "Local-only Manifest V3 Chrome extension that archives X timelines to IndexedDB with JSON/HTML/media exports.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "lint": "eslint .",
    "check": "node scripts/check.mjs",
    "icons": "node scripts/make-icons.mjs"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "fake-indexeddb": "^6.0.0",
    "fflate": "0.8.2",
    "happy-dom": "^15.0.0",
    "sharp": "^0.33.0",
    "vitest": "^3.0.0"
  }
}
